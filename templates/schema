import { Schema, SchemaDefinition } from 'mongoose';

const schemaDefinition: SchemaDefinition = {};

const {{pascalSingular}}Schema: Schema = new Schema(schemaDefinition);

export default {{pascalSingular}}Schema;